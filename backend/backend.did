service : {
  addScore: (nat, nat) -> ();
  getHighScores: () -> (vec record {
                              nat;
                              nat;
                            }) query;
  getRandomSentence: (text, nat) -> (text);
}
